#include "main.h"
#include <stdio.h>

/**
 * _isupper -Find if a given letter is uppercase
 * @c: the given letter
 *
 * Return: Return 1 if c is uppercase, 0 if otherwise
 */
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	return (0);
}
